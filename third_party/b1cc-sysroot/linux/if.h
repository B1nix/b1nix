#ifndef _LINUX_IF_H
#define _LINUX_IF_H
/* <linux/if.h>: the IFF_* flags and struct ifreq live in <net/if.h> for b1nix.
 * Added for the Chromium port (M60-62). */
#include <net/if.h>
#endif /* _LINUX_IF_H */
