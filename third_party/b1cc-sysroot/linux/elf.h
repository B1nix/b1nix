#ifndef B1NIX_U_LINUX_ELF_H
#define B1NIX_U_LINUX_ELF_H
/* Linux ports include <linux/elf.h> for the ELF headers, Nhdr types and NT_*
 * note constants; b1nix keeps those in <elf.h>, so forward there. */
#include <elf.h>
#endif
