#ifndef _LINUX_SOCKIOS_H
#define _LINUX_SOCKIOS_H
/* <linux/sockios.h>: socket ioctl request codes (SIOC*). b1nix keeps these in
 * <sys/ioctl.h>; alias to it. Added for the Chromium port (M60-62). */
#include <sys/ioctl.h>
#endif /* _LINUX_SOCKIOS_H */
