# b1nix test images: no logger daemon, one service at a time (the console is a
# single serial line, and interleaved service output is unreadable), and every
# service start printed.
rc_logger="NO"
rc_parallel="NO"
rc_verbose="yes"
