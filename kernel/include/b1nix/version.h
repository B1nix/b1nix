#ifndef B1NIX_VERSION_H
#define B1NIX_VERSION_H

/* Scheme: 0.<milestone>.<patch>. M95/M96 (loadable kernel modules) complete
 * here, but the tree already carries M97/M98 work at 0.98.x — the version never
 * goes backwards, so this lands as a patch bump rather than 0.96.0. */
#define B1NIX_VERSION_STR "0.98.11"

#endif
