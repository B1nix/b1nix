/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_IOCTL_H
#define LKPI_LINUX_IOCTL_H

/*
 * The ioctl number encoding, which is architecture's to define and which
 * <asm/ioctl.h> already does — this header exists because imported code spells
 * the include either way, and both must reach the same definitions.
 */
#include <asm/ioctl.h>

#endif
