/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_MMU_CONTEXT_H
#define LKPI_LINUX_MMU_CONTEXT_H
/* Borrowing a userspace address space from a kernel thread. Nothing imported
 * here does that: the userptr path that would is not built. */
#endif
