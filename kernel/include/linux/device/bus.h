/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_DEVICE_BUS_H
#define LKPI_LINUX_DEVICE_BUS_H
/* Upstream splits the bus type out of <linux/device.h>; here it is declared
 * there, so this is the split-out name pointing back. */
#include <linux/device.h>
#endif
