/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_RADIX_TREE_H
#define LKPI_LINUX_RADIX_TREE_H
#include <linux/xarray.h>
/* Upstream's radix tree is the xarray underneath, and the old names are kept
 * only for callers not yet converted. b1nix has the xarray; this is that name. */


#endif
