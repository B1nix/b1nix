/* SPDX-License-Identifier: MIT */
#ifndef LKPI_LINUX_MEMPOLICY_H
#define LKPI_LINUX_MEMPOLICY_H
/* NUMA allocation policy. b1nix's allocator has no node awareness — there is
 * one pool — so a policy has nothing to select between. */
struct mempolicy;
#endif
