/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_STRING_CHOICES_H
#define LKPI_LINUX_STRING_CHOICES_H

/* The str_*() helpers are kept in <linux/string_helpers.h>, where the 6.6 DRM
 * import expects them; 6.x filesystems include them by this name. */
#include <linux/string_helpers.h>

#endif
