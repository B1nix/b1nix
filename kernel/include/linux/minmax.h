/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_LINUX_MINMAX_H
#define LKPI_LINUX_MINMAX_H

/* min()/max()/clamp() are defined in <linux/kernel.h>; this header is the
 * name 6.x code includes for them. */
#include <linux/kernel.h>

#endif
