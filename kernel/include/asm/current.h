/* SPDX-License-Identifier: GPL-2.0-only */
#ifndef LKPI_ASM_CURRENT_H
#define LKPI_ASM_CURRENT_H
/* `current` is the running task; <linux/types.h> defines it for every file. */
#include <linux/types.h>
#endif
