#ifndef _LINUX_UNISTD_H
#define _LINUX_UNISTD_H
/* <linux/unistd.h>: the __NR_* syscall numbers. b1nix exposes the ones it has
 * via <sys/syscall.h>; alias. Added for the Chromium port (M60-62; lss). */
#include <sys/syscall.h>
#endif /* _LINUX_UNISTD_H */
