# Units that are expected to fail on an installed b1nix, one per line, each
# with the reason. DISTRO-SMOKE compares the machine's failed units against
# this list: a unit that is not here fails the lane, and a unit here that has
# stopped failing fails it too.
#
# The second half of that rule is the point. A list nobody prunes becomes the
# place where real breakage hides, so every entry has to keep earning its
# place, and closing a kernel gap means deleting a line here in the same
# change.
#
# Format: <unit name> <TAB or spaces> <why>

e2scrub_reap.service	needs LVM snapshots, which the block layer does not offer; harmless
